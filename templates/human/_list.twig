{% extends "index.twig" %}

{%~ block head %}
{{parent()}}
{% endblock %}

{% block body %}

{% block header %}
{{parent()}}
{% endblock %}

{% block article %}
	<section class="bg-white dark:bg-gray-900">
		<div class="py-8 px-4 mx-auto max-w-screen-xl lg:py-16 lg:px-6">
			<div class="mx-auto max-w-screen-sm text-center mb-8 lg:mb-16">
				<h1 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white"> 
				{% if entry.alternateHeadline %}
				{{ entry.alternateHeadline }}
				{% else %}
				{{ entry.title}} </h1>
				{% endif %}
				<p class="font-light text-gray-500 lg:mb-16 sm:text-xl dark:text-gray-400">These are the humans taken from us by the ADOC.</p>
			</div>
			<div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">


					{% set humans = craft.entries
						.section('humans')
						.all()
					%}

					{% for entry in humans %}

						{% set humanImage = entry.photos.one() %}
						{% set humanAdocFacility = entry.facility.one() %}
						{% set humanDOD = entry.dateOfDeath %}
						{% set humanNote = entry.note %}
						{% set humanAdocCOD = entry.causeOfDeath.one() %}
						{% set humanAdocReports = entry.reports.type('report') %}
						{% set humanUrl = entry.url %}

						<div class="bg-white rounded-lg border border-gray-200 shadow-sm dark:bg-gray-800 dark:border-gray-700">
							{% if humanImage %}
								<a href="{{ entry.url }}">
								{% do humanImage.setTransform('humanThumbnail') %}
									<img class="p-4 w-full rounded-lg" src="{{ humanImage.url }}"
									width="{{ humanImage.width }}"
									height="{{ humanImage.height }}" alt="{{ humanImage.title }}">
								</a>
							{% endif %}
							<div class="px-5 pb-5 mt-4">
								{% set name = "" %}
								{% if entry.lastName %}
									{% set name = entry.lastName %}
								{% endif %}
								{% if entry.firstName %}
									{% set name = name ~ ", " ~ entry.firstName %}
								{% endif %}
								{% if entry.nickName %}
									{% set name = name ~ " \"" ~ entry.nickName ~ "\"" %}
								{% endif %}
								{% if name %}
									<h3 class="text-xl font-bold tracking-tight text-gray-900 dark:text-white">
										<a href="{{ entry.url }}">{{ name }}</a>
									</h3>
								{% endif %}

								{% if entry.ais %}
								<p class="my-1 text-lg text-gray-500">
									<strong>AIS#</strong> {{entry.ais}}</p>
								{% endif %}
								{% if humanAdocFacility %}
									<span class="text-gray-500 text-lg">
										<strong>Facility:</strong>
										<a class="underline" href="{{ humanAdocFacility.url }}">{{ humanAdocFacility.title }}</a>
									</span>
								{% endif %}
								{% if humanNote %}
									<p class="my-2 font-light text-gray-500 dark:text-gray-400">
										<span class="font-medium">Details:</span> {{humanNote}}
									</p>
								{% endif %}
								{% if humanAdocCOD %}
									<p class="my-2 font-light text-gray-500 dark:text-gray-400">
										<span class="font-medium">COD:</span> {{humanAdocCOD}}
									</p>
								{% endif %}
								{% if humanAdocReports|length %}
									<h2 class="my-2 text-xl text-gray-500 dark:text-gray-400 font-bold">Reports</h2>
										<ul class="ml-4">
											{% for humanAdocReports in humanAdocReports %}
												<li class="list-decimal">
													{% set reportLink = humanAdocReports.reportLink %}
													{% if reportLink %}
															<a href="{{ reportLink }}">
																<h3 class="ml-2 mt-2 mb-1 underline font-light text-gray-500 dark:text-gray-400">{{ humanAdocReports.reportType.one().title }}</h3>
															</a>										
														{% else %}
														<h3 class="ml-2 mt-2 mb-1 font-light text-gray-500 dark:text-gray-400">{{ humanAdocReports.reportType.one().title }}</h3>
													{% endif %}
												</li>
											{% endfor %}
										</ul>
									{% endif %}
								</p>
							</div>
						</div>
					{% endfor %}
				</div>
			</div>
		</section>
{% endblock %}
{% block footer %}
	{{parent()}}
{% endblock %}
{% endblock %}
